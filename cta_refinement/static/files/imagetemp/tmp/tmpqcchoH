digraph {
	preApprovalApplication [label=preApprovalApplication]
	vehicleSelection [label=vehicleSelection]
	creditExpired [label=creditExpired]
	logged [label=logged]
	creditAccepted [label=creditAccepted]
	applicationRejected [label=applicationRejected]
	creditApproved [label=creditApproved]
	start [label=start]
	creditApplication [label=creditApplication]
	cancelled [label=cancelled]
	paymentEstimation [label=paymentEstimation]
	node [shape=none]
	0 [label=""]
	start -> logged [label="CS?login(True,{})"]
	logged -> preApprovalApplication [label="CS?preApproval(True,{})"]
	logged -> vehicleSelection [label="CS?selectVehicle(True,{})"]
	preApprovalApplication -> applicationRejected [label="SC!reject(True,{})"]
	preApprovalApplication -> creditApproved [label="SC!approved(True,{x})"]
	creditApproved -> vehicleSelection [label="CS?selectVehicle(x < 30,{})"]
	creditApproved -> creditExpired [label="SC!expired(x == 30,{})"]
	vehicleSelection -> paymentEstimation [label="CS?estimatePayment(True,{x})"]
	vehicleSelection -> cancelled [label="CS?cancel(True,{})"]
	paymentEstimation -> vehicleSelection [label="CS?reapplication(x < 30,{})"]
	paymentEstimation -> creditApplication [label="CS?fullCredit(x <= 24,{})"]
	paymentEstimation -> cancelled [label="SC!expired(x == 30,{})"]
	creditApplication -> applicationRejected [label="SC!reject(True,{})"]
	creditApplication -> creditAccepted [label="SC!accept(True,{})"]
	0 -> start
}
